import SwiftUI

// TODO: Task 10 replaces this placeholder with the real settings screen.
struct SettingsView: View {
    var body: some View {
        Text("Settings")
            .navigationTitle("Settings")
    }
}
