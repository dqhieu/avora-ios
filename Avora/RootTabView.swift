import SwiftUI

/// Placeholder — replaced by Task 7 with the real tab bar.
struct RootTabView: View {
    var body: some View {
        NavigationStack {
            Text("Signed in")
                .navigationTitle("Avora")
        }
    }
}
